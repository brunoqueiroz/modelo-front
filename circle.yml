machine:
  node:
    version: 0.10.28
  #services:
  #  - docker

dependencies:
  cache_directories:
    - node_modules
    - bower_components
  override:
    - npm install
    - bower install
test:
  override:
    - gulp test && gulp build
    - cp coverage/test-results.xml $CIRCLE_TEST_REPORTS/test-results.xml
